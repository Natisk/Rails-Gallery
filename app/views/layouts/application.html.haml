!!!
%html
  %head
    %title
      Catalog
    = stylesheet_link_tag    'application', media: 'all'
    = javascript_include_tag 'application'
    = csrf_meta_tags
%body
  .container
    .navbar.navbar-inverse.navbar-fixed-top
      .container
        = link_to 'Catalog', root_path, class: 'navbar-brand'
        %ul.nav.navbar-nav
          = nav_link_to('Categories', categories_path, 'categories')
          = nav_link_to('Comments', comments_path, 'comments')
        #user_nav.pull-right
          - if user_signed_in?
            %span Signed in as
            %i
              %b
                = current_user.name
            = link_to 'Sign out', destroy_user_session_path, :method => :delete, class: 'btn btn-primary btn-small'
            %span or
            = link_to 'Edit profile', edit_user_registration_path, class: 'btn btn-primary btn-small'
          - else
            = link_to 'Sign up', new_user_registration_path, class: 'btn btn-primary btn-small'
            = link_to 'Sign in', new_user_session_path, class: 'btn btn-primary btn-small'
            %span.facebook_login or Sign in with
            = link_to user_omniauth_authorize_path(:facebook) do
              = image_tag 'facebook.png', with: 32
    .content.row
      .main_content
        - if flash[:error]
          .col-lg-12
            %p.alert.alert-error
              = flash.now[:error]
        - if flash[:notice]
          .col-lg-12
            %p.alert.alert-success
              = flash.now[:notice]
        - if flash[:alert]
          .col-lg-12
            %p.alert.alert-info
              = flash.now[:alert]
        .col-lg-12
          = yield
    %footer
      .copy
        = "© #{Time.now.strftime('%Y')} Natisk, Inc."
      .org
        RoRteam