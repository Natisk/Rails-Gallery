= image_tag @image.img_name.url
.image_comments
  - @image.comments.each do |img_comment|
    - if img_comment.user
      = img_comment.user.name
    = img_comment.body
- if user_signed_in?
  .comments_form
    = render partial: 'comments/form'